import java.util.ArrayList;

public class bin {
    public int size;
    public int currentFill;

    public bin(int size){

    }

    // Check if the bin is full
    public boolean isFull(){

        }
    }

    // Increment how full the bin is by the number passed in


    // Fills the bin as much as possible and prints it
    public static void fillBin(ArrayList arr, bin bin) {

            }
        }

        System.out.println();
    }

}
